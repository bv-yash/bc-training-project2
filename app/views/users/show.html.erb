<h1><%= @user.name %></h1>

<p><%= @user.email %></p>

<p> All articles belong to this user will be shown here. </p>

<p> This is temporary </p>

<ul>
  <li><%= link_to "Edit Account", edit_user_path(@user) %></li>
  <li><%= link_to "Delete my Account", user_path(@user),
                method: :delete,
                data: { confirm: 'Are you sure?' } %></li>
</ul>